# This is a documentation-only repository.
# It contains research, decisions, and meeting notes — no deployable code.
# No CI or deployment is configured; this file exists only to silence
# GitHub's "no workflows" warning for new repos.
name: CI
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Documentation-only repo — no CI to run."
